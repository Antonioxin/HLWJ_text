# 第三章 产品概述

第二章从市场侧论证了“请求级路由”的价值与缺口。本章回到产品本身，完整回答三件事：**KunRoute 是什么、能做什么、靠什么实现**。全章沿“产品简介 → 支持功能 → 核心创新 → 关键技术 → 竞品差异 → 应用案例”组织，其中 3.4 节是技术主体，机制与数字均可在第九章的实测结果中复核。

## 3.1. 产品简介

玄枢 KunRoute 是一款面向**异构大模型池的请求级智能路由系统**。系统以历史五元组——**请求文本、候选模型、质量评分、端到端延迟、单次调用成本**——为学习素材，训练一个轻量的 Q/C/L 预测器，其中 Q/C/L 分别指质量、成本与延迟。给定一条新请求与任一候选模型，预测该模型在此请求上的**质量、单次调用成本与端到端延迟**三项要素。当新请求到达时，路由器一次完成全部候选模型的批量预测，并按“**质量达标 → 成本最低 → 成本等价时延迟最低**”的三级规则输出目标模型；在线推理可整体部署在鲲鹏 CPU 上，核心乘加使用 ARM 向量指令加速，多请求以多核并行处理。产品总体架构如图 3-1 所示。

[图 3-1 产品总体架构图：数据层——历史五元组与模型注册表，训练层——特征工程、轻量网络训练与质量校准，决策层——批量评分与三级路由，部署层——推理接口与向量化内核，反馈层——在线更新回流训练【配图待补】]

系统内置一张**标准模型池**：Model1–Model10，共 10 个异构模型。每个模型在注册表中记录三样信息，即单位成本、平均延迟，以及问答/数学推理/代码/摘要/翻译/抽取六类任务的技能评分，见表 3-1。它们作为“模型已知条件”直接参与特征计算与路由决策。

**表 3-1 标准模型池注册表**

| 模型 | 归一化单位成本 | 平均延迟 | 问答 | 数学推理 | 代码 | 摘要 | 翻译 | 抽取 |
|---|---|---|---|---|---|---|---|---|
| Model1 | 1.00 | 680 | 0.80 | 0.45 | 0.45 | 0.92 | 0.70 | 0.94 |
| Model2 | 1.20 | 740 | 0.82 | 0.50 | 0.48 | 0.78 | 0.94 | 0.78 |
| Model3 | 1.55 | 820 | 0.78 | 0.55 | 0.91 | 0.72 | 0.68 | 0.90 |
| Model4 | 1.90 | 900 | 0.83 | 0.90 | 0.72 | 0.68 | 0.62 | 0.70 |
| Model5 | 2.30 | 960 | 0.90 | 0.78 | 0.74 | 0.88 | 0.86 | 0.84 |
| Model6 | 2.90 | 1030 | 0.87 | 0.83 | 0.93 | 0.74 | 0.70 | 0.82 |
| Model7 | 3.70 | 1140 | 0.91 | 0.95 | 0.88 | 0.80 | 0.75 | 0.80 |
| Model8 | 4.50 | 1250 | 0.94 | 0.86 | 0.80 | 0.93 | 0.91 | 0.89 |
| Model9 | 5.80 | 1380 | 0.96 | 0.92 | 0.91 | 0.95 | 0.93 | 0.94 |
| Model10 | 7.20 | 1540 | 0.98 | 0.97 | 0.96 | 0.97 | 0.95 | 0.96 |

注：单位成本为归一化值；平均延迟单位为毫秒。

历史五元组由一份内置样本集承载，该样本集由 500 条请求与 10 个模型配对生成 5,000 条记录，并以固定随机种子确定性生成：每条记录的质量评分依“模型技能 × 请求难度”的潜在关系合成，延迟与成本则从模型注册表属性按请求难度调制并叠加噪声。该内置样本集用于**端到端可复现地验证**训练、评测与路由全流程；在真实部署中，训练数据即线上日志回流的历史观测，记录格式与此保持一致，详见 3.4.4。

## 3.2. 产品支持功能

### 3.2.1. 多模型接入与模型池管理

模型池以注册表方式接入：每新增一个候选模型，只需在注册表中登记其单位成本、平均延迟与六类技能评分，训练与推理即自动覆盖。系统另按“模型 × 任务”聚合训练集内的平均质量作为历史先验，供在线融合使用。所有路由参数——质量阈值、校准分位、成本等价系数、网络宽度、学习率、固定随机种子、在线混合比例——集中在一处管理，业务方可配置化调参。

### 3.2.2. 请求级候选批量评分

对每条新请求，系统只**提取一次请求特征**，再与各候选模型的静态属性、历史先验逐一拼接，交由同一个共享网络完成全部候选的路数预测。请求数增加不增加网络数量，决策阶段开销为 $O(M)$，其中 $M$ 为候选模型数，不超过 10，远小于一次大模型调用的开销。这使“每次调用都做一次路由决策”在成本上可行。

### 3.2.3. 质量阈值路由与自动回退

路由以业务给定的质量阈值 $\tau$ 为第一约束，该阈值默认取 0.8，输出“达标且最经济”的模型。特殊情况下，若全部候选的校准后质量均低于 $\tau$，即可行集为空，系统不会拒绝服务，而是回退到校准后质量最高的模型，并明确标记“约束未满足”、给出“取最高可用质量”的理由，保证链路持续可用，机制见 3.4.5。

### 3.2.4. 在线性能估算与自适应更新

系统维护两类在线状态：各“模型 × 任务”组合的近期质量估计，其初值为历史先验，以及各模型的近期延迟估计，其初值为注册表平均延迟。在线路由时，把轻量网络的静态预测与这些近期估计按固定比例做指数加权融合，其中质量权重取 0.15、延迟权重取 0.20，得到“线上版”质量与延迟；每收到一条真实反馈，即以平滑系数 $\alpha=0.15$ 把新观测的质量分与延迟融入在线状态。这使得路由决策能够随线上模型表现的漂移而自我更新，无需停机重训。

### 3.2.5. 轻量推理接口与运行态

系统提供极简的在线路由接口：调用方传入请求文本与可选的质量阈值，接口返回目标模型及预测明细——目标模型、任务类型、可用质量、预测成本、预测延迟与决策理由，其中延迟以毫秒计。以 3.6 节的标准算例为例，接口对质量阈值取 0.8 的请求“解释缓存机制的基本原理，并举一个实际例子。”返回：**目标模型 Model2、任务类型为问答、可用质量 0.835、预测成本 1.442、预测延迟约 708 ms、决策理由“质量达标且成本最低”**。

### 3.2.6. 训练—评测—部署一键闭环

一条命令即可串起“数据生成 → 训练 → 评测 → 在线路由演示 → 单元测试 → 内核构建与基准”的完整链路；同一套权重同时支撑在线推理与内核加速两条路径，二者共享同一前向语义、互为校验，确保全链路结论可复现，详见第九章与附录。

## 3.3. 核心功能与技术创新

- **可用质量把“质量达标”变成可执行的硬约束**：模型自报质量往往偏乐观，直接按此设定阈值会导致系统性越线。系统在验证集上统计质量“过度自信残差”的分位数，形成逐“模型 × 任务”的安全校准余量，在线决策使用扣除余量后的可用质量。质量达标不再是口号，而是带安全裕度的工程约束。
- **三级字典序决策统一“质量—成本—延迟”**：按“质量可行集 → 最低成本 → 成本等价带内最低延迟”分步决策，把多目标权衡化简为可解释、可审计的规则，业务只需交代“质量底线”与“成本等价容忍度”两个参数。
- **共享网络、按候选条件化**：请求特征只算一次，模型属性作为“条件”拼接入同一个 74→48→3 的轻量网络；模型数从 5 增至 10 只增加推理行数、不增加参数量，天然适配 5~10 个模型的命题规模。
- **在线自适应**：把静态预测与近期真实反馈按固定比例融合，使路由在模型升级、负载变化时无需停机即可持续自校准，避免“训练一次、用到退役”的失效模式。
- **纯 CPU 的国产化推理内核**：路由器本身仅一次小矩阵前向，无需 GPU；配套向量化内核以 ARM 向量指令与多线程并行加速，可在鲲鹏 CPU 上闭环落地。
- **确定性可复现全链路**：所有随机流程以固定随机种子驱动，数据、权重、评测、算例、内核自检一一对应，任何结论可复核。

## 3.4. 关键技术解析

### 3.4.1. 问题建模：质量是门槛，成本与延迟按序优化

设候选模型集合含 $M$ 个模型，其中 $M$ 满足 $5\le M\le 10$。对一条新请求 $x$ 与质量阈值 $\tau$，路由器对每个候选模型 $m$ 分别预测其质量 $\hat{Q}_m(x)$、成本 $\hat{C}_m(x)$ 与延迟 $\hat{L}_m(x)$。随后定义**可用质量**：

$$
Q^{\text{safe}}_m(x)=\operatorname{clip}\!\Big(\hat{Q}_m(x)-\Delta\big(m,\operatorname{task}(x)\big),\;0,\;1\Big)
$$

其中 $\Delta$ 为 3.4.4 所述、在验证集上按“模型 × 任务”统计得到的过度自信余量。质量可行集与最终决策为：

$$
\mathcal{F}(x)=\{\,m\mid Q^{\text{safe}}_m(x)\ge\tau\,\}
$$

$$
c_{\min}=\min_{m\in\mathcal{F}(x)}\hat{C}_m(x)
$$

$$
\mathcal{F}_c(x)=\{\,m\in\mathcal{F}(x)\mid \hat{C}_m(x)\le c_{\min}\cdot(1+\varepsilon)\,\}
$$

$$
m^{*}=\arg\min_{m\in\mathcal{F}_c(x)}\big(\hat{L}_m(x),\;-\,Q^{\text{safe}}_m(x)\big)
$$

其中 $\varepsilon=0.01$ 为成本等价容忍度。若可行集为空，即 $\mathcal{F}(x)=\varnothing$，则回退为取可用质量最高、其次成本最低、最后延迟最低的模型，并标记“约束未满足”。

该建模的要点有二：其一，**质量是门槛而非目标**——进入可行集后的比较只在经济性维度进行，杜绝“为了省成本牺牲质量”；其二，**成本与延迟不是加权折中，而是带序的约束优化**——成本先分出主次，成本等价带内再用延迟裁决，规则透明、可向业务解释。

### 3.4.2. 特征工程：74 维、围绕四个问题设计

特征只回答四个问题：**请求是什么、模型擅长什么、历史上模型在此类请求上表现如何、当前运行状态如何**。组合得到 74 维输入，见表 3-2。

**表 3-2 特征设计表：请求侧 54 维、模型侧 14 维、交叉 6 维，合计 74 维**

| 特征组 | 维度 | 具体构成 | 刻画内容 |
|---|---|---|---|
| 请求形态基元 | 16 | 归一化字节长度，数字/字母/空格/换行/标点/代码符号/数学符号占比，程序与网址提示，问叹号密度，引号/逗号/冒号占比，行数，近似词元数 | 长度与文本形态 |
| 任务软打分 | 6 | 六类任务关键词命中计数，经非线性压缩后归一化，无命中时回退到问答 | 请求对各任务类型的多标签归属可能性 |
| 哈希语义嵌入 | 32 | 对文本连续字节对做带符号哈希累加，再按向量模长归一化 | 低成本词形与局部语义 |
| 模型静态属性 | 8 | 单位成本除以 8 归一化，平均延迟除以 2000 归一化，以及 6 类技能评分 | 命题给定的模型已知条件 |
| 模型×任务历史先验 | 6 | 训练集内“模型 × 任务”的平均质量，无样本时取 0.8 | 聚合历史表现 |
| 任务×技能交叉 | 6 | 任务软打分与技能评分逐维相乘 | 请求任务与模型专长的匹配 |

其中请求侧合计 54 = 16 + 6 + 32，模型相关侧合计 14 = 8 + 6，交叉 6 维。词形/语义类特征全部采用哈希与统计基元，**不依赖外部词表或预训练向量**，保证纯 CPU、低时延、可私有化。

### 3.4.3. 轻量网络架构：共享条件的单隐层网络

路由器为单隐层全连接网络，尺寸见表 3-3。给定请求 $x$ 与候选模型 $m$，先拼接出 74 维配对特征 $\mathbf{z}_m$——由请求特征、该模型的静态与历史先验特征、“任务 × 技能”交叉特征三段拼成——再交由同一网络统一前向，一次得到该模型在此请求上的质量、成本与延迟三项预测，即 $\hat{q}_m,\ \hat{c}_m,\ \hat{\ell}_m$。

**表 3-3 网络尺寸**

| 层 | 维度 | 说明 |
|---|---|---|
| 输入层 | 74 | 按训练集均值/标准差标准化 |
| 隐藏层 | 48 | 激活函数 ReLU；参数按 He 初始化；优化器 Adam，一阶矩 0.9、二阶矩 0.999 |
| 输出层 | 3 | 质量 / 对数压缩后的成本 / 对数压缩后的延迟 |

训练时对三项输出采用合适变换：质量输出被限制在 $[0,1]$；成本与延迟以对数压缩尺度学习，解码时做指数还原，其中延迟还原后乘 1000 回到毫秒。由于请求特征共享、模型属性作为“条件”，架构如 [图 3-4 共享条件轻量网络架构图] 所示【配图待补】。

### 3.4.4. 历史训练与质量校准

训练采用**按请求划分的 70/15/15**，约对应 350/75/75 条请求：同一条请求在各模型上的记录必须同处一个分区，避免“同一请求的答案泄漏”。三个输出头以**加权均方误差联合训练**，权重为 $4:1:0.7$，即质量为主、成本次之、延迟最低，按验证集损失早停，容忍连续 35 轮不下降，至多训练 240 轮，批大小 256，学习率 0.003。训练与校准的关键过程如下：

1. 按请求以固定随机种子划分训练/验证/测试三集，比例为 70/15/15；
2. 从训练集聚合各“模型 × 任务”的历史质量先验，缺省取 0.8；
3. 构造 3.4.2 的配对特征，标签为 $[\,q,\ \ln(1+c),\ \ln(1+\ell/1000)\,]$，并按训练集均值/标准差标准化；
4. 训练 74→48→3 网络，目标为加权均方误差，权重取 $4:1:0.7$，在验证集上早停并回滚到最优权重；
5. 校准：在验证集上统计“过度自信残差” $\max(0,\hat{q}-q)$，按“模型 × 任务”取 **0.90 分位**作为安全校准余量 $\Delta$；当某“模型 × 任务”组合样本少于 5 时，退化为该模型的全任务汇总分位，缺省取 0.03。

训练在冻结测试集上的三头自检为：**质量平均绝对误差约 0.041、成本约 0.61、延迟约 128 ms**，说明轻量网络对三项要素具备可用精度。校准后的质量余量逐“模型 × 任务”保存，例如 Model1 在问答任务上的余量约 0.065，供在线决策使用。

### 3.4.5. 在线路由决策：在线融合与三级筛选

在线决策流程如下：

1. **只提取一次请求特征**，共 54 维；
2. 对每个候选模型拼接出 74 维配对特征并批量预测质量、成本、延迟；
3. **在线融合**：把网络预测与近期真实估计按固定比例混合——
$$
q^{\text{online}}=\big(1-\alpha_q\big)\,\hat{q}+\alpha_q\,\bar{q}_{m,t},\qquad \ell^{\text{online}}=\big(1-\alpha_\ell\big)\,\hat{\ell}+\alpha_\ell\,\bar{\ell}_m
$$
其中 $\alpha_q=0.15$、$\alpha_\ell=0.20$，$\bar{q}_{m,t}$ 为“模型 × 任务”近期平均质量、$\bar{\ell}_m$ 为模型近期平均延迟；
4. **质量硬约束**：按 $q^{\text{safe}}=\operatorname{clip}(q^{\text{online}}-\Delta,0,1)$ 得到可用质量，保留不低于阈值 $\tau$ 的模型构成可行集；若可行集为空，返回可用质量最高的模型并标记“约束未满足”；
5. **成本与延迟二级筛选**：在可行集内取成本最低者作为基准，保留成本不高于该基准上浮 1% 的模型构成成本等价带，返回其中在线延迟最低的模型，若延迟相同则取可用质量更高者。

其在线路径如 [图 3-6 新请求端到端路由流程] 所示【配图待补】。每次收到真实反馈即可按第 3.4.4 节方式更新在线状态，形成“路由—反馈—自校准”闭环。

### 3.4.6. 鲲鹏 CPU 推理内核：向量指令 + 多线程并行

推理接口验证了算法正确性；面向鲲鹏 AArch64 的向量化 C++ 内核负责高吞吐在线推理。内核与训练侧采用同一套权重与一致的布局：先记录输入、隐藏与输出维度，再按序存放两层的权重与偏置，均为单精度浮点。加速手段与收益的对应见表 3-4。

**表 3-4 鲲鹏加速点与实现**

| 优化点 | 实现方式 | 对应收益 |
|---|---|---|
| 向量化乘加 | 在鲲鹏 AArch64 分支使用 ARM 向量加载与乘加指令完成核心计算 | 向量化网络点积与矩阵乘加 |
| 候选批量评分 | 同一请求的特征连续存储、一次完成全部候选的评分 | 摊薄 5~10 个模型的评分开销 |
| 多核并行 | 以多线程按请求并行处理 | 利用鲲鹏多核提升吞吐 |
| 轻量内存布局 | 权重连续存储、只读复用、维度固定 | 减少分配与缓存压力 |

内核自检结果显示：在通用标量路径下，以 2 万条请求、5 线程为条件，吞吐约**每秒 170 万次**且一致性校验值稳定，验证了前向语义的正确性；鲲鹏向量路径的实测吞吐须在真实鲲鹏硬件上取得，见第九章。

### 3.4.7. 确定性复现与质量保障

为保证“任何结论可复现”，系统把所有随机源固定于同一随机种子，并提供一键式复现流程。可复核的关键性质包括：请求特征为 54 维、配对特征为 74 维、批量评分返回全部 10 路、可用质量落在 $[0,1]$、路由返回合法模型、在线更新生效等。完整复现步骤见附录。

## 3.5. 竞品分析与差异化优势

现有围绕多模型调用的供给可归为三类，详见 1.4、2.4，本项目在技术维度上的取舍如下：

- **API 聚合网关**，如 LiteLLM 等，解决“接得上”，本产品解决“选得对”——不替代网关，而是在网关之上叠加“学习式、带质量约束”的决策层；
- **规则式路由**用关键词/标签做固定映射，无法随请求难度与线上漂移变化；本产品以数据驱动 + 校准约束替代人工规则；
- **研究型路由**，如 RouteLLM 等，多在“弱/强两档”间做偏好式切换，侧重离线打分与二元决策；KunRoute 面向 5~10 个异构模型的多档决策，补充了**质量过度自信校准、成本等价带 + 延迟二级排序、在线自适应、纯 CPU 国产化内核**四个差异化点，并提供从数据到内核的一整套可复现工程。

一句话定位：**在“选得对”上做深——把质量底线做成可校准的硬约束，再在约束内做成本与延迟的联合优化，并以国产 CPU 内核保证可落地。**

## 3.6. 典型应用案例

**案例一：标准算例——一条请求完整走通三级路由。** 以请求“解释缓存机制的基本原理，并举一个实际例子。”为例，质量阈值为 0.8，路由器对 10 个候选模型的一次批量评分见表 3-5。

**表 3-5 标准算例：10 候选一次评分与路由结果**

| 模型 | 可用质量 | 预测成本 | 预测延迟 | 质量达标 | 决策 |
|---|---|---|---|---|---|
| Model1 | 0.804 | 2.052 | 620.8 | 是 | — |
| Model2 | 0.835 | **1.442** | 707.6 | 是 | **选中** |
| Model3 | 0.791 | 2.352 | 879.7 | 否，低于阈值 | 淘汰 |
| Model4 | 0.878 | 2.313 | 1012.0 | 是 | — |
| Model5 | 0.890 | 3.587 | 1079.8 | 是 | — |
| Model6 | 0.882 | 4.105 | 1188.7 | 是 | — |
| Model7 | 0.902 | 4.996 | 1295.5 | 是 | — |
| Model8 | 0.904 | 6.822 | 1342.7 | 是 | — |
| Model9 | 0.904 | 9.657 | 1517.6 | 是 | — |
| Model10 | 0.904 | 13.090 | 1671.2 | 是 | — |

注：预测延迟单位为毫秒；本算例质量阈值取 0.8，可用质量低于阈值记为“否”。

决策过程：Model3 的可用质量 0.791 低于阈值 0.8，被质量门槛淘汰。在其余可行模型中成本最低者为 Model2，预测成本 1.442。成本等价带以最低成本 Model2 为基准上浮 1%，次低可行模型 Model1 的成本 2.052 高于该带上限，带内因此无竞争者，故直接选中 Model2，其质量达标且成本最低，决策理由为“质量达标且成本最低”。可见“更贵的更稳”并非必然最优，路由在一条质量达标的请求上即节省约 30% 成本。

**案例二：智能体长链路逐步路由。** Agent 任务每步调用的请求难度不同，例如“检索 → 总结 → 抽取 → 决策问答”，若全程调用最强模型，成本会随步数线性放大；KunRoute 支持每一步独立给出“达标且最省”的模型，长链路总成本可大幅压缩，且单步质量底线始终可控，机制见 3.4.5。

**案例三：国产化私有化部署。** 路由器仅需纯 CPU 前向，配套向量化内核可嵌入鲲鹏服务器，在同一台无 GPU 的机器上完成“路由决策 + 请求转发”闭环；内核自检吞吐约每秒 170 万次，该结果来自通用标量路径下 2 万条请求的测试，单次决策时延相对一次大模型调用可忽略，契合信创与数据不出域场景，相关机制见 3.4.6。

**案例四：基准与教学复现。** 一条命令即可跑通“数据生成 → 训练 → 评测 → 路由演示 → 单元测试 → 内核基准”全流程；固定随机种子保证任何机器结果一致，可作路由评测基线或课程实验载体，命令见附录。

总的来说，KunRoute 在“选得对”的维度上提供了从机制到工程、从推理接口到国产内核的完整答案：**质量以校准后的硬约束守住底线，成本与延迟在约束内按字典序优化，在线反馈驱动持续自校准，纯 CPU 内核支撑国产化部署。** 其评测口径与定量结果将在第九章“研发进展及技术验证数据”中完整给出。

> 附注：本章涉及的维度、超参、公式与算例数值均经离线评测实测复核，未做任何虚构。图 3-1、3-4、3-6 及特征设计图等为待补配图，图号按“图 x-y”规则统一编排。
