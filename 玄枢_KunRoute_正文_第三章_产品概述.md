# 第三章 产品概述

第二章从市场侧论证了"请求级路由"的价值与缺口。本章回到产品本身，完整回答三件事：**KunRoute 是什么、能做什么、靠什么实现**。全章沿"产品简介 → 支持功能 → 核心创新 → 关键技术 → 竞品差异 → 应用案例"组织；其中 3.4 节是技术主体，所有机制均可还原到 `src/`、`cpp/`、`config/` 与 `results/` 中的具体实现。

## 3.1. 产品简介

玄枢 KunRoute 是一款面向**异构大模型池的请求级智能路由系统**。系统以历史五元组 `(query, model, quality_score, latency, cost)` 为学习素材，训练一个轻量 MLP（称为 Q/C/L 路由器）：给定一条新请求与任一候选模型，预测该模型在此请求上的**质量（quality）、单次调用成本（cost）与端到端延迟（latency）**三项要素。当新请求到达时，路由器一次完成全部候选模型的批量预测，并按"**质量达标 → 成本最低 → 成本等价时延迟最低**"的三级规则输出目标模型 `model*`；在线推理可整体部署在鲲鹏 CPU 上，核心乘加使用 NEON 向量化，多请求以多核并行处理。产品总体架构如图 3-1 所示。

[图 3-1 产品总体架构图：数据层（历史五元组 + 模型注册表）→ 训练层（特征工程、LightMLP 训练、质量校准）→ 决策层（批量评分、三级路由）→ 部署层（Python 推理 API + C++/NEON 内核）→ 反馈层（EWMA 在线更新回流训练）【配图待补】]

系统内置一张**标准模型池**：Model1~Model10，共 10 个异构模型。每个模型在注册表中记录三样信息——单位成本、平均延迟、以及问答/数学推理/代码/摘要/翻译/抽取六类任务的技能标签（表 3-1，源文件 `config/model_registry.csv`），它们作为"模型已知条件"直接参与特征计算与路由决策。

**表 3-1 标准模型池注册表（`config/model_registry.csv`）**

| 模型 | 单位成本（归一化） | 平均延迟（ms） | skill_qa | skill_math | skill_code | skill_summary | skill_translation | skill_extraction |
|---|---|---|---|---|---|---|---|---|
| Model1 | 1.00 | 680 | 0.80 | 0.45 | 0.45 | 0.92 | 0.70 | 0.94 |
| Model2 | 1.20 | 740 | 0.82 | 0.50 | 0.48 | 0.78 | 0.94 | 0.78 |
| Model3 | 1.55 | 820 | 0.78 | 0.55 | 0.91 | 0.72 | 0.68 | 0.90 |
| Model4 | 1.90 | 900 | 0.83 | 0.90 | 0.72 | 0.68 | 0.62 | 0.70 |
| Model5 | 2.30 | 960 | 0.90 | 0.78 | 0.74 | 0.88 | 0.86 | 0.84 |
| Model6 | 2.90 | 1030 | 0.87 | 0.83 | 0.93 | 0.74 | 0.70 | 0.82 |
| Model7 | 3.70 | 1140 | 0.91 | 0.95 | 0.88 | 0.80 | 0.75 | 0.80 |
| Model8 | 4.50 | 1250 | 0.94 | 0.86 | 0.80 | 0.93 | 0.91 | 0.89 |
| Model9 | 5.80 | 1380 | 0.96 | 0.92 | 0.91 | 0.95 | 0.93 | 0.94 |
| Model10 | 7.20 | 1540 | 0.98 | 0.97 | 0.96 | 0.97 | 0.95 | 0.96 |

历史五元组由 `data/sample_history.csv` 提供（500 个 query × 10 模型 = 5,000 条记录），由 `src/generate_sample_data.py` 以固定随机种子确定性生成：每条记录的 `quality_score` 依"模型技能 × 请求难度"的潜在模型合成，`latency`、`cost` 则从模型注册表属性按请求难度调制并加入噪声。该内置数据集用于**端到端可复现地验证**训练、评估与路由全流程；在真实部署中，`D` 即线上日志回流的历史观测，数据格式保持一致（详见 3.4.4 与附录）。

## 3.2. 产品支持功能

### 3.2.1. 多模型接入与模型池管理

模型池以注册表方式接入：每新增一个候选模型，只需在 `config/model_registry.csv` 追加一行（单位成本、平均延迟、六维技能标签），训练与推理即自动覆盖。历史先验（`artifacts/model_priors.json`）按 model×task 聚合训练集平均质量生成，供在线融合使用。所有路由参数——质量阈值、校准分位、成本等价系数、网络宽度、学习率、随机种子、在线融合比例——集中在 `config/router_config.json` 一处管理，业务方可配置化调参。

### 3.2.2. 请求级候选批量评分

对每条新请求，系统只**提取一次请求特征**，再与 10 个候选模型的静态属性、历史先验逐一拼接，交由同一个共享网络完成 `M` 路预测（`score_all`）。请求数增加不增加网络数量，决策阶段开销为 `O(M)`，M≤10 时远小于一次大模型调用的开销。这使"每次调用都做一次路由决策"在成本上可行。

### 3.2.3. 质量阈值路由与自动回退

路由以业务给定的质量阈值 τ（默认 0.8）为第一约束，输出"达标且最经济"的模型。特殊情况下，若所有候选的校准后质量均低于 τ（可行集为空），系统不会拒绝服务，而是回退到校准后质量最高的模型并给出 `constraint_unmet=True` 与 `reason='HIGHEST_QUALITY'`，保证链路持续可用（见 3.4.5 算法 2）。

### 3.2.4. 在线性能估算与自适应更新

系统维护两个在线状态：model×task 的近期质量估计（初值为历史先验）与每模型的近期延迟估计（初值为注册表平均延迟）。在线路由时，MLP 的静态预测与这些近期估计按固定比例（质量 0.15、延迟 0.20）做指数加权融合，得到"线上版"质量与延迟；每收到一条真实反馈，即可通过 `update_online` 以 EMA（默认 α=0.15）把新观测（质量分/延迟）融合进在线状态。这使得路由决策能够随线上模型表现漂移而自我更新，无需停机重训。

### 3.2.5. 轻量推理 API 与运行态

系统提供极简 HTTP 接口 `POST /route`：请求体携带 `query` 与可选 `quality_threshold`，响应返回目标模型及预测明细（模型、任务、`pred_quality`、`quality_safe`、`pred_cost`、`pred_latency`、决策理由）：

```
POST /route
{ "query": "解释缓存机制的基本原理，并举一个实际例子。", "quality_threshold": 0.80 }

200 OK
{ "model": "Model2", "task": "qa", "quality_safe": 0.835,
  "pred_cost": 1.442, "pred_latency_ms": 707.6, "reason": "QUALITY_PASS_MIN_COST" }
```

（示例取自 `results/standard_case.json` 标准算例的实际输出，详见 3.6。）

### 3.2.6. 训练—评估—部署一键闭环

`run_all.sh` 串起数据生成 → 训练 → 评估 → 在线路由演示 → 单元测试 → C++ 内核构建与基准的完整链路；训练产物（权重、标准化参数、先验、校准表、数据划分）统一写入 `artifacts/`，评测与算例输出写入 `results/`。同一份权重同时导出为 NumPy 格式（供 Python 推理）与紧凑二进制 `weights.bin`（供 C++ 内核加载），两套推理路径使用同一套权重与同一前向语义，互为校验。

## 3.3. 核心功能与技术创新

- **质量校准（`Q_safe`）把"质量达标"变成可执行的硬约束**：模型自报质量往往乐观，直接设定阈值会导致系统性越线。系统在验证集上统计质量"过预测残差"的分位数，形成逐 model×task 的校准余量 Δ，在线决策使用 `Q_safe = clip(Q̂ − Δ, 0, 1)`（`router.py._predict_pair`）。质量达标不再是一个口号，而是一个有安全裕度的工程约束。
- **三级字典序决策统一"质量—成本—延迟"**：`质量可行集 → 最低成本 → 成本等价带内最低延迟`，把多目标权衡化简为可解释、可审计的分步规则，业务只需交代"质量底线 τ"与"成本等价容忍度 ε"。
- **共享网络、按候选条件化**：请求特征只算一次，模型属性作为"条件"拼接入同一个 74→48→3 的轻量网络，模型数从 5 增至 10 只增加推理行数、不增加参数量，天然适配 5~10 个模型的命题规模。
- **在线 EWMA 自适应**：把静态预测与近期真实反馈按固定比例融合，使路由在模型升级、负载变化时无需停机即可持续自校准，避免"训练一次、用到退役"的失效模式。
- **纯 CPU 的国产化推理内核**：路由器本身仅一次小矩阵前向，无需 GPU；配套 `cpp/router_kernel.cpp` 提供 AArch64 NEON 向量化与 OpenMP 请求级并行，可在鲲鹏 CPU 上闭环落地。
- **确定性可复现全链路**：所有随机流程以固定 `seed=20260901` 驱动（`router_config.json`），数据、权重、评测、算例、内核自检一一对应，任何结论可复核。

## 3.4. 关键技术解析

### 3.4.1. 问题建模：质量是门槛，成本与延迟按序优化

设模型集合 `M={1,…,M}`（5≤M≤10）。对一条新请求 `x` 与质量阈值 τ，路由器对每个候选模型 `m` 预测 `Q̂_m(x)`、`Ĉ_m(x)`、`L̂_m(x)`。随后定义**校准后质量**：

```
Q_safe_m(x) = clip( Q̂_m(x) − Δ(m, task(x)) , 0 , 1 )
```

其中 Δ 为 3.4.4 所述、在验证集上按 model×task 统计得到的过预测余量。质量可行集与最终决策为：

```
F(x)      = { m | Q_safe_m(x) ≥ τ }                     # 质量硬约束
c_min     = min_{m∈F(x)} Ĉ_m(x)
F_cost(x) = { m ∈ F(x) | Ĉ_m(x) ≤ c_min·(1+ε) }          # 成本等价带，ε=0.01
model*    = argmin_{m∈F_cost(x)} ( L_m(x) , −Q_safe_m(x) )   # 带内取延迟最低，其次质量最高
若 F(x) 为空：model* = argmax_m ( Q_safe_m(x) , −Ĉ_m(x) , −L̂_m(x) )   # 回退，置 constraint_unmet=True
```

该建模的要点有二：其一，**质量是门槛而非目标**，进入可行集后的比较只在经济性维度进行，杜绝"为了省成本牺牲质量"；其二，**成本与延迟不是加权折中，而是带序的约束优化**，成本先分出主次、等价带内再用延迟裁决，规则透明、可向业务解释。（实现见 `router.py.route()`。）

### 3.4.2. 特征工程：74 维、围绕四个问题设计

特征只回答四个问题：**请求是什么、模型擅长什么、历史上模型在此类请求上表现如何、当前运行状态如何**。组合得到 74 维输入（`feature_extractor.py`：`INPUT_DIM=74`），结构见表 3-2。

**表 3-2 特征设计表（`feature_extractor.py`，74 = 54 + 8 + 6 + 6）**

| 特征组 | 维度 | 具体构成 | 刻画内容 |
|---|---|---|---|
| 请求形态基元 | 16 | 归一化字节长度、数字/字母/空格/换行/标点/代码符号/数学符号占比、JSON 提示、URL 提示、问叹号密度、引号/逗号/冒号占比、行数、近似 token 数 | 长度与文本形态 |
| 任务软打分 | 6 | 六类关键词命中计数，经 `exp(min(s,4))` 后归一化（0 命中回退到 qa） | 请求属于各任务类型的概率（多标签保留） |
| 哈希语义嵌入 | 32 | UTF-8 字节 bigram 带符号哈希累加 + L2 归一化 | 低成本词形与局部语义 |
| 模型静态属性 | 8 | 归一化单位成本 `cost/8`、归一化平均延迟 `ms/2000`、6 维技能标签 | 命题给定的模型已知条件 |
| 模型×任务历史先验 | 6 | 训练集内 model×task 平均 `quality_score`（无样本缺省 0.8） | 聚合历史表现 |
| 任务×技能交叉 | 6 | `task_prob × skill` 逐维相乘 | 请求任务与模型专长的匹配 |

其中请求侧合计 54 = 16 + 6 + 32，模型相关侧合计 14 = 8 + 6，交叉 6 维。词形/语义类特征全部采用哈希与统计基元，**不依赖外部词表或预训练向量**，保证纯 CPU、低时延、可私有化。

### 3.4.3. 轻量网络架构：共享条件 LightMLP

路由器为单隐层全连接网络（`train_router.py.TinyMLP`），尺寸见表 3-3。给定请求 `x` 与模型 `m`，先拼出 74 维配对特征 `z_m = concat(query_feature, model_feature_m, task_prob × skill_m)`，再统一前向：

```
(q̂_m, ĉ_m, l̂_m) = LightMLP( z_m )
```

**表 3-3 网络尺寸（`config/router_config.json`：`hidden_dim=48`）**

| 层 | 维度 | 说明 |
|---|---|---|
| 输入层 | 74 | 训练集均值/标准差标准化 |
| 隐藏层 | 48 | ReLU；He 初始化；Adam（β₁=0.9, β₂=0.999） |
| 输出层 | 3 | quality / log1p(cost) / log1p(latency/1000) |

输出在训练时对三要素取合适变换：质量头输出经 `clip` 回 [0,1]；成本头与延迟头以 `log1p` 压缩尺度（解码时 `expm1` 还原，延迟还原后×1000 得到 ms）。由于请求特征共享、模型属性作"条件"，架构如 [图 3-4 共享条件 LightMLP 架构图] 所示【配图待补】。

### 3.4.4. 历史训练与质量校准

训练采用 **query 级 70/15/15 划分**（350/75/75 个 query，见 `artifacts/model_meta.json` 与 `train_router.py.split_queries`）：同一 query 在多个模型上的记录必须同处一个分区，避免"同一请求的答案泄漏"。三头以**加权 MSE 联合训练**，输出头权重为 4:1:0.7（质量为主、成本次之、延迟最低），按验证集损失早停（patience=35，上限 240 epoch，batch=256，lr=0.003）。模型关键过程见算法 1。

**算法 1 训练与校准（`train_router.py`）**
```
输入：历史 D、注册表、配置 cfg
1  按 query 以 seed 划分 Train/Valid/Test（70/15/15）
2  从 D 聚合 model×task 历史质量先验 priors（缺省 0.8）
3  构造配对特征（表 3-2），标签 Y=[q, log1p(c), log1p(l/1000)]
4  用训练集均值/标准差标准化 X、Y
5  TinyMLP(74→48→3)，Adam，加权 MSE（权重 4:1:0.7）迭代
6  在 Valid 上按损失早停，回滚到最优权重
7  校准：Valid 上算过预测残差 max(0, q̂−q)，按 model×task
   取 0.90 分位得 Δ（样本<5 时取该模型汇总分位，缺省 0.03）
8  输出 artifacts/：router_weights.npz、weights.bin、priors、calibration、meta、split
```

训练在冻结测试集上的三头自检（`model_meta.json.self_check`）为：质量 MAE≈0.041、成本 MAE≈0.61、延迟 MAE≈128 ms，说明轻量模型对三要素具备可用精度。校准后的质量余量逐 model×task 写入 `artifacts/calibration.json`（如 Model1 在 qa 任务上的余量约 0.065），供在线决策使用。

### 3.4.5. 在线路由决策：EWMA 融合与三级筛选

在线决策流程（`router.py.route()`、`_predict_pair()`）如算法 2。

**算法 2 在线路由（`router.py`）**
```
输入：新请求 query，质量阈值 τ
1  x ← 一次提取请求特征（54 维，只做一次）
2  对 m=1..M：z_m ← concat(x, model_m, cross)；批量预测 q̂,ĉ,l̂
3  q_online ← (1−α_q)·q̂ + α_q·近期质量(model_m, task)     # α_q=0.15
4  l_online ← (1−α_l)·l̂ + α_l·近期延迟(model_m)           # α_l=0.20
5  q_safe ← clip(q_online − Δ(model_m, task), 0, 1)
6  F ← { m | q_safe ≥ τ }；若 F 为空：返回 q_safe 最高者（constraint_unmet=True）
7  c_min ← min ĉ(m), m∈F；F_cost ← { m∈F | ĉ(m) ≤ c_min·(1+0.01) }
8  返回 F_cost 中 l_online 最低者（平手取 q_safe 更高者）
```

其在线路径如 [图 3-6 新请求端到端路由流程] 所示【配图待补】。每次收到真实反馈可调用 `update_online(model_id, query, quality_score, latency)` 做 EMA 更新，形成"路由—反馈—自校准"闭环。

### 3.4.6. 鲲鹏 CPU 推理内核：NEON + OpenMP

Python 端验证了算法正确性；面向鲲鹏 AArch64 的 C++ 内核（`cpp/router_kernel.cpp`）负责高吞吐在线推理。内核以与训练侧一致的布局加载 `weights.bin`（文件头 `<4sIII>`：魔数 `KRT1` + 输入/隐藏/输出维度，随后按行优先写 W1、b1、W2、b2 的 float32）。加速手段与命题要求的对应见表 3-4。

**表 3-4 鲲鹏加速点与实现（`cpp/router_kernel.cpp`）**

| 优化点 | 实现方式 | 对应收益 |
|---|---|---|
| NEON 乘加 | `__aarch64__` 分支用 `vld1q_f32`/`vmlaq_f32`/`vaddvq_f32` | 向量化 LightMLP 点积与矩阵乘加 |
| 候选批量评分 | 同一请求特征连续存储、一次完成 M 路评分 | 摊薄 5~10 个模型的评分开销 |
| 多核并行 | `#pragma omp parallel for schedule(static)` 请求级并行 | 利用鲲鹏多核提升吞吐 |
| 轻量内存布局 | 连续 float32 数组、只读复用权重、维度固定 | 减少分配与缓存压力 |

内核单测级自检输出（`results/kernel_selfcheck.txt`）在便携标量路径下达到约 1.7e6 QPS（20,000 请求、5 线程）且 `checksum` 稳定，验证了前向语义的正确性。构建命令如下，AArch64 自动启用 `-march=armv8-a+simd`：

```
cmake -S cpp -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
OMP_NUM_THREADS=N ./build/kunroute_bench artifacts/weights.bin 100000
```

### 3.4.7. 确定性复现与质量保障

为保证"任何结论可复现"，工程包把随机源全部钉死在 `seed=20260901`（`generate_sample_data.py`、`router_config.json`），并提供一键流水与单元测试（`tests/test_router.py`，覆盖请求特征维度 54、配对特征 74、批量评分返回 10 路且 `quality_safe∈[0,1]`、路由返回合法模型、在线更新生效）。一键复现顺序与完整命令见附录。

## 3.5. 竞品分析与差异化优势

现有围绕多模型调用的供给（详见 1.4、2.4）可归为三类，本项目在技术维度上的取舍如下：

- **API 聚合网关（如 LiteLLM 等）**解决"接得上"，本产品解决"选得对"——不替代网关，而是在网关之上叠加"学习式、带质量约束"的决策层；
- **规则式路由**用关键词/标签做固定映射，无法随请求难度与线上漂移变化；本产品以数据驱动 + 校准约束替代人工规则；
- **研究型路由（如 RouteLLM 等）**多在"弱/强两档"间做偏好式切换，侧重离线打分与二元决策；KunRoute 面向 5~10 个异构模型的多档决策，补充了**质量过预测校准、成本等价带 + 延迟二级排序、EWMA 在线自适应、纯 CPU 国产化内核**四个差异化点，且提供从数据到内核的一整套可复现工程。

一句话定位：**在"选得对"上做深——把质量底线做成可校准的硬约束，再在约束内做成本与延迟的联合优化，并以国产 CPU 内核保证可落地。**

## 3.6. 典型应用案例

**案例一：标准算例——一条请求完整走通三级路由。** 以请求"解释缓存机制的基本原理，并举一个实际例子。"（τ=0.8）为例，路由器对 10 个候选的一次批量评分见表 3-5（数据取自 `results/standard_case.json`）。

**表 3-5 标准算例：10 候选一次评分与路由结果（τ=0.8）**

| 模型 | quality_safe | pred_cost | pred_latency (ms) | 质量达标 | 决策 |
|---|---|---|---|---|---|
| Model1 | 0.804 | 2.052 | 620.8 | 是 | — |
| Model2 | 0.835 | **1.442** | 707.6 | 是 | **选中** |
| Model3 | 0.791 | 2.352 | 879.7 | 否（<τ） | 淘汰 |
| Model4 | 0.878 | 2.313 | 1012.0 | 是 | — |
| Model5 | 0.890 | 3.587 | 1079.8 | 是 | — |
| Model6 | 0.882 | 4.105 | 1188.7 | 是 | — |
| Model7 | 0.902 | 4.996 | 1295.5 | 是 | — |
| Model8 | 0.904 | 6.822 | 1342.7 | 是 | — |
| Model9 | 0.904 | 9.657 | 1517.6 | 是 | — |
| Model10 | 0.904 | 13.090 | 1671.2 | 是 | — |

决策过程：Model3 校准后质量 0.791 < 0.8，被质量门槛淘汰；在其余可行模型中成本最低者为 Model2（1.442）；Model2 成本低于次低可行模型 Model1（2.052）×（1+1%），等价带内无竞争者，故直接选中 Model2——其质量达标、成本最低，输出理由 `QUALITY_PASS_MIN_COST`。可见"更贵的更稳"并非必然最优，路由在一条质量达标请求上即省下约 30% 成本。

**案例二：智能体长链路逐步路由。** Agent 任务每步调用的请求难度不同（如"检索→总结→抽取 JSON→决策问答"），若全程调用最强模型成本随步数线性放大；KunRoute 支持每步独立给出"达标且最省"的模型，长链路总成本可大幅压缩，且单步质量底线始终可控（机制见 3.4.5）。

**案例三：国产化私有化部署。** 路由器仅需纯 CPU 前向，配套 C++/NEON 内核可嵌入鲲鹏服务器，在同一台无 GPU 的机器上完成"路由决策 + 请求转发"闭环；内核自检 QPS 约 1.7e6（便携标量、20,000 请求），决策时延相对一次大模型调用可忽略，契合信创与数据不出域场景（见 3.4.6）。

**案例四：基准与教学复现。** `run_all.sh` 一键跑通数据→训练→评估→路由→单测→内核基准；固定 seed 保证任何机器结果一致，可作路由评测基线或课程实验载体（命令见附录）。

总的来说，KunRoute 在"选得对"的维度上提供了从机制到工程、从 Python 到 C++ 内核的完整答案：**质量以校准后的硬约束守住底线，成本与延迟在约束内按字典序优化，在线反馈驱动持续自校准，纯 CPU 内核支撑国产化部署。** 其评测口径与定量结果将在第九章"研发进展及技术验证数据"中完整给出。

> 附注：本章所有维度、超参、公式、文件路径与算例数值均溯源至 `KunRoute_竞赛提交包/` 的 `src/`、`cpp/`、`config/`、`artifacts/`、`results/` 与 `tests/`；未做任何虚构。图 3-1、3-4、3-6 及特征设计图等为待补配图，图号将按 `图x-y` 规则统一编排。
