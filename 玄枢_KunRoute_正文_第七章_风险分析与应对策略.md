# 第七章 风险分析与应对策略

第六章给出的财务模型建立在技术基线之上，本章则自曝其短：先识别 KunRoute 在大模型路由场景下的真实风险，再以代码已落地的机制（EMA 在线校准、校准余量、兜底回退、纯 CPU 内核等）作为应对证据。章首先要放一张风险总览示意图（图 7-1）以"概率 × 影响"象限呈现，随后按"风险类别 → 风险点 → 应对策略"两段式展开。

**图7-1 风险地图示意【新画】**

建议按二维象限绘制：横轴为"发生概率（低 / 中 / 高）"、纵轴为"影响程度（低 / 中 / 高）"。各类风险点落位建议如下——**高概率高影响**：上游模型 API 质量漂移、校准余量过预测；**高概率中影响**：选中延迟 P95 波动、客户对"非最优路由"敏感；**中概率高影响**：模型池结构性变化、数据回流合规；**中概率中影响**：竞品同质化、人才流失；**低概率高影响**：上游厂商断供、合规监管收紧。绘制时可按象限配色（红/橙/黄/绿）并标注对应风险编号（R1–R10，见表 7-1）。

## 7.1. 技术风险

### 7.1.1. 路由质量预测偏差与校准漂移

**风险描述**：Q/C/L 路由器以历史五元组训练（`train_router.py`），若上游模型 API 在生产环境的真实质量/延迟分布与训练集发生偏移（如厂商悄然升级或降级），`pred_quality` 会出现系统性偏差，导致 `quality_safe` 估计失真，最终触发质量越线或选错档位。当前实测 `quality_pass_rate=0.9733`、`quality_violation_rate=0.0267`（来源：`results/sample_evaluation.json`），仍有 2.67% 的越线残余，是路由技术不可回避的固有风险。

**应对策略**：
1. **校准余量机制**：`train_router.py` 在训练后对每 (model×task) 计算 `calibration.json` 中的 `quality_margin_quantile`（默认 0.90 分位），`router.py` 在 `route()` 中以 `quality_safe = pred_quality - 校准余量` 作为可行集准入条件，把过预测风险前置吸收为保守边界。
2. **EMA 在线自校准**：`router.py` 的 `update_online()` 以 α=0.15 的指数加权移动平均把新观测融合进在线质量/延迟估计，再按 `online_quality_blend` 与 MLP 预测加权混合，使路由器在生产环境持续自适应漂移（详见 3.4.4 在线校准）。
3. **质量阈值双约束**：默认 `tau=0.8`（`config/router_config.json`），可在私有化部署时按场景收紧，给质量约束留出更大安全裕度。

### 7.1.2. 在线延迟波动与 P95 SLA 风险

**风险描述**：当前 `selected_latency_p95=1653.19ms`（来源：`results/sample_evaluation.json`），是路由所选模型在测试集上的延迟分布尾部分位。生产环境中，上游模型 API 的尾部延迟、网络抖动与队列等待会进一步放大 P95，可能突破客户 SLA（如客服场景要求 ≤1s）。

**应对策略**：
1. **成本等价带取低延迟**：`route()` 在 `quality_safe >= tau` 的可行集中，取成本 ≤ `cmin*(1+cost_tie_epsilon)` 的等价带，再在其中选延迟最低者，结构性地把延迟作为第三优化目标与质量、成本并列。
2. **在线延迟 EMA**：与质量校准同步，`update_online()` 对延迟做 EMA 估计，使路由器感知到某个上游模型出现尾部延迟恶化时主动绕开。
3. **降级回退**：当某条路由链路 P95 超阈值时，可配置回退到次优档位或最强档位兜底（对应 `constraint_unmet=True` 的回退路径）。

### 7.1.3. 上游模型 API 失败与断供

**风险描述**：异构模型池 Model1–Model10 依赖外部 API，单一模型厂商断供、限流或下线时，路由器若无兜底将无法完成请求。

**应对策略**：`router.py` 的 `route()` 在无可行解时显式置 `constraint_unmet=True` 并回退到最高 `quality_safe` 的模型，作为"宁愿多花钱也要回答回来"的兜底策略；私有化部署建议在模型池注册表（`config/model_registry.csv`）中配置主备模型对，断供时自动切换。

### 7.1.4. C++ 内核跨平台一致性风险

**风险描述**：C++ 内核（`cpp/router_kernel.cpp`）需在 AArch64（鲲鹏）与便携标量两种路径下复现 MLP 前向，二者浮点累加顺序不同可能导致 `checksum` 微小偏差，进而影响路由决策的跨平台一致性。当前 `kernel_selfcheck.txt` 显示 `path=portable scalar` 的 baseline，AArch64 端实测 QPS 尚未在真实鲲鹏硬件上取得【待补】。

**应对策略**：
1. `router_kernel.cpp` 在 `__aarch64__` 分支用 NEON intrinsic（`vld1q_f32` / `vmlaq_f32` / `vaddvq_f32`）做核心乘加，标量分支做兼容回退，二者数学等价；
2. 内核输出 `checksum` 字段供跨平台一致性自检，可在 CI 中对 AArch64 与标量路径的 `checksum` 设置容差断言；
3. `tests/test_router.py` 中的 `test_query_feature_shape`/`test_pair_feature_shape` 断言 54/74 维，保护特征维度与权重二进制格式（`KRT1` 魔数）一致性。

## 7.2. 市场与竞争风险

### 7.2.1. 路由方案同质化与开源竞争

**风险描述**：AI 网关类产品（Portkey、LiteLLM、Cloudflare AI Gateway）可能加入"模型选择"能力，使路由决策成为网关的免费附属功能，挤压本项目 API 收入空间。

**应对策略**：本项目与网关的本质差异在于"决策层"而非"转发层"——网关按 Token 转发不加价，路由按"质量校准 + 成本等价带 + 延迟最优"做有理论的选模决策，并以校准余量与 EMA 自校准作为差异化技术壁垒（详见 4.2.6 定价依据）。市场策略上以"开源内核 + 可复算评测"建立开发者心智，与网关生态互补而非正面竞争。

### 7.2.2. 客户对"非最优路由"敏感

**风险描述**：实测 `oracle_route_accuracy=0.4533`（来源：`results/sample_evaluation.json`），即路由所选模型与用 ground-truth 后验最优选择的一致率约 45.3%。部分客户可能将"非 oracle 路由"理解为"选错"，影响采用意愿。

**应对策略**：以评测口径引导客户预期——`oracle_route_accuracy` 是"最优匹配"指标，而客户真正关心的应是"质量是否达标 + 成本是否更低"。当前 `quality_pass_rate=97.3%` 与 `relative_cost_vs_all_strong=36.3%` 才是客户价值口径；向客户披露 ROI 时应同时呈现"匹配率"与"达标率 + 降本率"双指标，避免单一口径误导。

### 7.2.3. 替代品威胁（最强档兜底）

**风险描述**：客户可简单采用"全部走最强模型"策略规避路由复杂度，是路由方案最直接的替代品。

**应对策略**：以 `relative_cost_vs_all_strong=36.3%` 作为可复算降本证据——即同等质量约束下，路由方案综合成本仅为"全用最强"的 36.3%，对成本敏感客户具备不可替代性。

## 7.3. 运营风险

### 7.3.1. 模型池变更与再训练成本

**风险描述**：上游模型版本迭代、新增或下线时，路由器需基于新数据回流再训练，带来算力与人力成本。

**应对策略**：训练流程轻量化（单隐层 MLP，hidden=48，纯 CPU 即可训练，`requirements.txt` 仅依赖 numpy），单次再训练成本极低；`train_router.py` 已固化 `seed=20260901` 确定性复现流程，便于在客户侧快速重训。

### 7.3.2. 在线数据回流与隐私

**风险描述**：`update_online()` 的 EMA 校准依赖生产环境真实 (query, model, quality, latency, cost) 回流，可能涉及客户查询隐私。

**应对策略**：私有化部署中数据不出域；托管 API 中仅回流聚合统计量（EMA 后的均值/方差），不回流原始 query 文本；具体脱敏口径以客户合规要求为准【待补】。

### 7.3.3. 标杆 POC 交付与口碑

**风险描述**：早期标杆客户的交付质量直接决定开发者社区与后续客户的采用意愿，POC 失败会形成口碑风险。

**应对策略**：以 `run_all.sh` 端到端可复现流水线（数据→训练→评估→单测→C++ 基准）作为 POC 标准 demo，降低交付不一致风险；POC 报告须附带 `results/sample_evaluation.json` 真实指标，作为可审计证据。

## 7.4. 法律与数据合规风险

### 7.4.1. 数据隐私与安全

**风险描述**：路由器虽不存储客户 Token，但仍需读取 query 文本做特征工程（54 维查询特征中含字符统计、关键词打分与哈希 bigram），存在客户查询数据驻留风险。

**应对策略**：私有化部署中 query 处理全部在客户机房完成；托管 API 中 query 仅在路由决策过程中驻留内存，决策完成即释放；如客户合规要求高，可关闭哈希 bigram 特征（受 INPUT_DIM 断言保护，需同步更新权重二进制）。

### 7.4.2. 知识产权保护

**风险描述**：开源 C++ 内核与特征工程算法公开后，存在被竞品 fork 后包装为自有方案的风险。

**应对策略**：核心算法（校准余量、成本等价带、EMA 在线混合）以专利或商业秘密保护；开源部分采用适当的开源协议（协议选择【待补】）保留商标与商业版差异化权利。

### 7.4.3. 内容审核与伦理

**风险描述**：路由器所选模型若返回违规内容，客户可能将责任归因于路由决策。

**应对策略**：路由器只做"选模"，不做"内容生成"，内容审核责任仍归上游模型厂商；私有化部署中建议客户在路由后置内容审核模块，与路由决策解耦。

## 7.5. 资金风险

### 7.5.1. 研发与运营成本

**风险描述**：纯 CPU 训练与推理虽降低了算力成本，但研发人力（AI 算法 + C++ 架构师）属于高薪岗位，前期固定成本压力较大。

**应对策略**：以 6.1.3 成本结构表严格控制研发费用占比；以首批标杆客户与私有化授权大单尽早覆盖人力固定成本；具体阈值【待补】。

### 7.5.2. 融资与盈利

**风险描述**：若首轮融资金额或时点不及预期，可能影响产品迭代节奏与生态建设进度。

**应对策略**：以已有可复现工程包与真实评测数据作为融资信任基础；备选银行授信与政府专项（如信创、算力调度方向）作为补充资金来源，具体额度【待补】。

为便于评审横向对照，下表汇总上述风险点及对应对策（见表 7-1）。

**表 7-1 风险—概率/影响—对策矩阵【建议】**

| 编号 | 风险类别 | 风险点 | 发生概率 | 影响程度 | 主要对策（对应代码/章节） |
|---|---|---|---|---|---|
| R1 | 技术 | 路由质量预测偏差与校准漂移 | 中 | 高 | 校准余量（`calibration.json`，分位 0.90）+ EMA 在线自校准（`update_online`，α=0.15）+ 质量阈值 `tau=0.8`（3.4.4 / 7.1.1） |
| R2 | 技术 | 在线延迟 P95 波动 | 中 | 中 | 成本等价带取低延迟（`cost_tie_epsilon`）+ 延迟 EMA + 降级回退（`route()` 兜底，7.1.2） |
| R3 | 技术 | 上游模型 API 失败与断供 | 低 | 高 | `constraint_unmet=True` 回退到最高 `quality_safe`；私有化主备模型对配置（7.1.3） |
| R4 | 技术 | C++ 内核跨平台一致性 | 低 | 中 | NEON intrinsic 与标量分支数学等价；`checksum` 跨平台容差断言；`test_*_feature_shape` 维度断言（7.1.4） |
| R5 | 市场 | 路由方案同质化与开源竞争 | 高 | 中 | 决策层 vs 转发层差异化；校准余量与 EMA 作为技术壁垒；开源内核建立开发者心智（7.2.1） |
| R6 | 市场 | 客户对"非最优路由"敏感 | 中 | 中 | 以 `quality_pass_rate=97.3%` 与 `relative_cost_vs_all_strong=36.3%` 作为客户价值口径，弱化单一 `oracle_route_accuracy`（7.2.2） |
| R7 | 市场 | "全用最强"替代品威胁 | 中 | 中 | 以 `relative_cost_vs_all_strong=36.3%` 作为可复算降本证据（7.2.3） |
| R8 | 运营 | 模型池变更与再训练成本 | 中 | 中 | 训练流程轻量化（hidden=48，纯 CPU）+ `seed=20260901` 确定性复现（7.3.1） |
| R9 | 运营 | 在线数据回流与隐私 | 中 | 中 | 私有化数据不出域；托管 API 仅回流 EMA 聚合统计量（7.3.2） |
| R10 | 法律 | 数据隐私与内容审核 | 低 | 高 | query 不持久化；路由与内容审核解耦；私有化部署在客户机房完成（7.4.1 / 7.4.3） |

综上，KunRoute 的核心风险集中于"上游模型漂移引发的路由偏差"与"路由决策被同质化"两条主线，前者以代码已落地的校准余量 + EMA 自校准 + 兜底回退三层机制兜底，后者以"决策层差异化 + 可复算评测 + 开源心智"组合应对。表中所有"概率/影响"等级为团队建议值，可在评审中以历史运维数据校准【待补】。
